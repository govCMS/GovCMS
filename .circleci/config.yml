version: '2.1'

orbs:
  tests: 'govcms/tests@dev:first'

workflows:
  test:
    jobs:
      - tests/validate-composer
      - tests/drupal-practices
      - tests/build:
          requires:
            - tests/validate-composer
      - tests/basic:
          requires:
            - tests/build
      - tests/phpunit:
          requires:
            - tests/build
      - tests/cypress:
          requires:
            - tests/build
